language: objective-c
matrix:
  include:
    - os: osx
      osx_image: xcode7.3
    - os: osx
      osx_image: xcode8
before_script:
  - export LANG=en_US.UTF-8
install: bundle
env:
  - CUSTOM_DEVICE_SET_PATH=true
  - CUSTOM_DEVICE_SET_PATH=false
script:
  - bundle exec rake
